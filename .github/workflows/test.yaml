name: Test
on:
  push:
    branches:
      - main

env:
  SLACK_STREAM: true

jobs:
  test:
    name: First comes the tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  build:
    needs:
      - test
    runs-on: ubuntu-latest
    steps:
      - run: echo 'hello'
      - run: sleep 1000
