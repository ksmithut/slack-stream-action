name: Test
on:
  push:
    branches:
      - main
      - __slack-stream__

jobs:
  test:
    name: First comes the tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  build:
    needs:
      - test
    runs-on: ubuntu-latest
    steps:
      - run: echo 'hello'
